PORT=3000

# Optional. Set both values on Render to keep rooms alive after server restarts.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Rooms are deleted automatically after this many seconds.
ROOM_TTL_SECONDS=86400
